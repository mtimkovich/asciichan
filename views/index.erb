<!DOCTYPE html>
<html>
  <head>
    <title>/ascii/</title>
    <link rel="stylesheet" href="main.css">
  </head>

  <body>
    <h1>/ascii/</h1>

    <% if not @gmap_img.nil? %>
      <img id="map" src="<%= @gmap_img %>">
    <% end %>

    <form method="post">
      <table>
        <tr>
          <label>
            <td>title</td>
            <td><input type="text" name="title" value="<%= @title %>"></td>
          </label>
        </tr>

        <br>

        <tr>
          <label>
            <td>art</td>
            <td><textarea name="art"><%= @art %></textarea></td>
          </label>
        </tr>

        <br>

        <tr>
          <td></td>
          <td>
            <div id="error"><%= @error %></div>
          </td>
        </tr>

        <tr>
          <td></td>
          <td><input type="submit"></td>
        </tr>
      </table>
    </form>

    <br>

    <% @arts.each do |art| %>
      <b><%= art.title %></b>
      <pre><%= art.art %></pre>
    <% end %>
  </body>
</html>

